from operator import index

# track flipped cards each turn
flipped = []

'''def on_click(index):
    if len(flipped) >= 2:
        return # block a third flip
    flipped.append(index)
    revealCard(index)
    if len(flipped) == 2:
        checkMatch()'''